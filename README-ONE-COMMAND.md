# Engineer Platform — One Command

Requirements: Node.js LTS and Docker Desktop (running).

Windows: double-click `START-ENGINEER.bat`.

Terminal: `npm start`

macOS/Linux: `./start-engineer.sh`

This starts PostgreSQL, prepares/seeds the database, then starts the API and frontend together.
